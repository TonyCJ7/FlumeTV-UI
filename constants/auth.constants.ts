/** Matches backend `MIN_PASSWORD_LENGTH` in `auth.handler.ts`. */
export const MIN_PASSWORD_LENGTH = 8;

/** Query param on `/config` from Stremio `GET /:token/configure` redirect (backend Step 30). */
export const CONFIGURE_UUID_QUERY_PARAM = "uuid";
