/** Max length for `configName` on POST/PUT — matches backend `postConfig.handler.ts`. */
export const CONFIG_DISPLAY_NAME_MAX_LEN = 200;

/** Minimum time the active toggle stays disabled after click (debounce + stable loader). */
export const ACTIVE_TOGGLE_MIN_LOADING_MS = 750;
